//
//  AppColors.swift
//  QuickSip
//
//  Created by Seun Adekunle on 4/12/25.
//

import SwiftUI

enum AppColors {
    // Primary colors - keeping the green theme but with matcha inspiration
    static let primary = Color(red: 0.4, green: 0.75, blue: 0.4) // Matcha green
    static let secondary = Color.white
    
    // Matcha-inspired colors
    static let matchaDark = Color(red: 0.2, green: 0.4, blue: 0.2) // Dark matcha
    static let matchaLight = Color(red: 0.7, green: 0.85, blue: 0.7) // Light matcha
    static let matchaCream = Color(red: 0.96, green: 0.96, blue: 0.9) // Cream color
    
    // Text colors
    static let textPrimary = Color(red: 0.15, green: 0.3, blue: 0.15) // Dark green
    static let textSecondary = Color(red: 0.4, green: 0.5, blue: 0.4) // Medium green
    
    // Background colors
    static let background = Color(red: 0.95, green: 0.98, blue: 0.95) // Light cream with green tint
    static let secondaryBackground = Color(red: 0.92, green: 0.95, blue: 0.9) // Light green-beige
    
    // Accent colors
    static let accent = Color(red: 0.3, green: 0.6, blue: 0.3) // Deeper matcha green
}
